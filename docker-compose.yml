services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: daralearn
    ports:
      - "5433:5432"
    volumes:
      - daralearn-postgres:/var/lib/postgresql/data

volumes:
  daralearn-postgres:
